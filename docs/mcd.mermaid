erDiagram

    USER {
        int id 
        string name
        string email
        string role
    }

    direction {
        sint user_id 
    }

    collaborators {
        int user_id
    }

    project_manager {
        user_id
    }

    team {
        int id
        int user_id 
        string name
    }

    financier{
        int id
        int user_id 
    }


    PROJECT {
        int id 
        int author_id
        string title
        text form_data
        string status
        datetime created_at
    }

    project_proposal {
        int id 
        int project_id
        datetime created_at
    }

user ||--|| cololaborators : be
colaborators }o--|| PROJECT : created
project_proposal ||--o{} PROJECT : checking

archive_of_rejected {
    int id
    int project_id
    datetime rejected_at
}

archive_of_rejected ||--o{} PROJECT : rejected

    COMMENT {
        int id
        int project_id 
        int author_id 
        text content
        datetime created_at
    }


projects_approved_for_funding {
    int id
    int project_id
    int financier_id
    datetime approved_at
}

projects_approved  {
    int id 
    int project_id
    int team_id
    int project_manager_id
    int comment_id
    datetime approved_at
}

archive_of_completed {
    int id
    int project_id
    datetime completed_at
}

project_manager ||--o{ comment : make
projects_approved ||--o{} PROJECT : be  
projects_approved ||--|| team : realized_by
projects_approved ||--|| project_manager : managed_by
projects_approved ||--o{} COMMENT : have

projects_approved_for_funding ||--o{} PROJECT : be_funded_by
projects_approved_for_funding ||--o{} financier : funded_by