### Context details
We have 2 pages, one where when we click the request for modification, it redirects the user to a special formulaire with a check box across each inputs, the form should look exactly the same as the original proposition form except with the checkbox next to each input, and a comment inputs below each original input. when a director clicks and marks one of the parts of the formulaire, "wrong" then submits the wrong formulaire.

then there's another formulaire, where the user can see the comments of the directions below each input that's invalid, so the user only needs to modify stuff that's invalid. not the whole formulaire.

### The issue
My colleague will implement the first part, however I do not have any idea regarding on how I can implement the correclation between which input is wrong with the checkbox, which formulaire it is and who it belongs too. then when a user re submits the formulaire it goes back into propositions with the now corrected data, on the correct inputs.

